# Secret Share Bot - Production Dependencies
# Install these packages in order to avoid conflicts

# Core dependencies (install first)
python-telegram-bot==21.0.1
python-dotenv==1.0.0
requests==2.31.0
aiohttp==3.9.1

# Database
supabase>=2.9.0

# AI/ML and Image Generation (install after core)
replicate==0.22.0
elevenlabs==0.2.26
pydub==0.25.1

# Additional utilities
Pillow==10.1.0
numpy==1.24.3

# Development and testing (optional for production)
pytest==7.4.3
pytest-asyncio==0.21.1